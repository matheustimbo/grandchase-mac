=== GRANDCHASE 100% GRÁTIS NO MAC (sem CrossOver) — RECEITA ===
Verificado 2026-06-12, M1 Max, macOS 27. Entra no lobby e renderiza 3D normal.

RENDER (confirmado por vmmap no processo rodando):
  GrandChase (D3D9) -> wined3d -> OpenGL -> AppleMetalOpenGLRenderer -> Metal
  NÃO usa DXVK. NÃO depende de override de MoltenVK.

COMPONENTE (único externo):
- wine-proton 10  (do GameHub) -> traz wined3d, o caminho OpenGL->Metal, o MoltenVK
  embutido (lib/libMoltenVK.dylib) e renderiza o webhelper CEF da Steam (login).
- Prefix: ~/Games/gc-proton  (Steam + jogo + runtimes VC++ genuínos + overrides Themida)

O QUE FAZ CADA PEÇA:
- wine-proton 10 -> roda o app no macOS 27 + login da Steam (CEF) + wined3d (render 3D)
- Themida ("Wrong DLL present") -> runtimes VC++ genuínos (system32) + overrides 'native'
  no registro (HKCU\Software\Wine\DllOverrides). O gatilho é o runtime VC++, NÃO o d3d9.
- Login Steam: pelo webhelper (UI) na 1ª vez; depois sessão salva.

ENV DE LAUNCH:
  WINEPREFIX=~/Games/gc-proton
  DYLD_FALLBACK_LIBRARY_PATH=<wine-proton>/lib:<wine-proton>/lib/wine/x86_64-unix
  WINEESYNC=1   WINEENGINE_GPTK_DXGI_SPOOF_ENABLED=1
  Lança: <wine> steam.exe -silent  (espera login)  ;  depois  <wine> steam.exe -applaunch 985810

NÃO É NECESSÁRIO (provado em runtime — ver "dead ends" no README):
  DXVK (qualquer versão), override de MoltenVK 1.4.1, dxvk.conf, WINEDLLPATH.
  O render passa por wined3d->OpenGL; nada disso chega a ser carregado.
